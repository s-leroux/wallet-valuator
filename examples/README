## Run one example file:

```console
sh$ node -r source-map-support/register build/examples/portfolio-valuation-tabular.mjs
```

## Run the example suite

```console
sh$ EXAMPLES=1 npm test -- -grep "Example"
```
