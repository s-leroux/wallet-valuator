import * as chai from "chai";
import chaiAsPromised from "chai-as-promised";

chai.use(chaiAsPromised);
const assert = chai.assert;

describe("DataSource", function () {
  beforeEach(function () {});

  describe("constructor", () => {
    it("should retrun value");
  });
});
